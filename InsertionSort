void InsertionSort(int arr[], int sz) {
	int i, j, key;
	
	for (i = 1; i < sz; i++) {
		key = arr[i];
		j = i - 1;
		while (j >= 0 && arr[j] > key) {
			arr[j + 1] = arr[j];
			 j--;
		}
		if (arr[j + 1] != key) {
			arr[j + 1] = key;
		}
	}
}
